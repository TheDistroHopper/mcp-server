# MCP Server

## Usage
```json
{
  "mcpServers": {
    "tasks": {
      "command": "npx",
      "args": ["-y", "https://github.com/TheDistroHopper/mcp-server.git"],
      "env": {
        "API_BASE": "API base URL"
      }
    }
  }
}
```